public class AllStudentsGradeInquiry {
    public static char gradeInquiry() {
       /* if (필수 ? 선택 ? &&(95 <= 점수 && 점수 <= 100)){
            return 'A';
        } else if (필수 ? 선택 ? &&(90 <= 점수 && 점수 <= 94)){
            return 'B';
        } else if (필수 ? 선택 ? &&(80 <= 점수 && 점수 <= 89)){
            return 'C';
        } else if (필수 ? 선택 ? &&(70 <= 점수 && 점수 <= 79)){
            return 'D';
        } else{
            return 'F';
        }*/return 'a'; // 지워야함
    }
}

public class SpecificStudentAverage {
    public static void averageCalculate() {
        int sum = 0;
        int average = 0;

        ##############                                                 ##############

        if (List '과목'.student.status().equalsIgnoreCase("Green")){
            List '과목'.student.score;
            List Students.getstatus


            // 상태 필터링 -> 과목 필터링
            // 메서드 green인 학생 뽑는 메서드, 과목별로 나누는 메서드 두 가지 이용

            for (Student temp : list) {
                if (temp.getStatus().equalsIgnoreCase("Green")) {
                    if () // 과목
                }
                sum += temp.getStudentMap.get(Subject)

            }

##############                                                 ##############
            for (int i : ScoreRegist.yj) {
                sum += i;
            }
            average = sum / ScoreRegist.yj.size();
            System.out.println("평균 : " + average);

        }
    }

    public static void pickStatusUp(String status) {
        for (Student temp : StudentRepository.students) {
            if (temp.getStatus().equalsIgnoreCase(status)) {

            }

        }
    }
}